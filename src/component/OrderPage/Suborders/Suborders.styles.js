import styled from '@emotion/styled/macro';
import {
    Box,
    Divider,
    Button,
    Flex
} from "@chakra-ui/react"


export const Evaluation = styled.div`
  border: 2px solid #f0f0f0;
  border-radius: 5px;
  width: 500px;
}
`;

export const TenPercent = styled(Box)`
    width: 10%
}
`;

export const TwentyPercent = styled(Box)`
    width: 20%
}
`;

export const FortyPercent = styled(Box)`
    width: 40%
}
`;

export const NowDivider = styled(Divider)`
    margin: 20px 0
}
`;

export const MarginButton = styled(Button)`
    border-radius: 20px;
    margin-right: 20px;
}
`;

export const PositionFlex = styled(Flex)`
    position: absolute;
}
`;

